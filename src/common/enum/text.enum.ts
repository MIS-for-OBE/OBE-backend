export enum TEXT_ENUM {
  Success = 'success',
}

export enum DESCRIPTION {
  SUCCESS = 'Successful operation.',
  BAD_REQUEST = 'Invalid input paremeters.',
  UNAUTHORIZED = 'Authentication fails. Token is invalid or expires.',
  FORBIDDEN = 'Access is forbidden.',
  NOT_FOUND = 'Data is not found.',
}
